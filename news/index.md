# Changelog

## prolfquasaint 0.1.5

- Declared the non-CRAN dependencies `prolfqua` and `saintexpressbin` in
  `Remotes` so the package installs from a fresh clone (previously only
  `saintexpress` was listed).
- Prepared package metadata, documentation, vignettes, and CI for
  Bioconductor-style checks.
